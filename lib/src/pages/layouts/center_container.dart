import 'package:flutter/material.dart';


class CenterContainerPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        margin: EdgeInsets.all(50.0),
        color: Colors.amber,
      ),
   );
  }
}